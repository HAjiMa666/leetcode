整数反转的主要思路就是对传进入的数取余
```js
    while (x !== 0) {
        //传进入的数除余数拿到最后一位
        a = x % 10
        //js和其他语言不同,没有类型判断,除出来的数都是
        //浮点数,所以需要取整 x===0的时候就能退出循环
        //说明全部除完了
        x = parseInt(x / 10);
        //sum将他们*10反加起来,即整数反转 
        sum = sum * 10 + a;
    }
```